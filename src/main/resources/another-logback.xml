<configuration>
    <appender name="MY-STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoder 默认配置为PatternLayoutEncoder -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="my-logback" level="INFO" additivity="false">
        <appender-ref ref="MY-STDOUT"/>
    </logger>

</configuration>